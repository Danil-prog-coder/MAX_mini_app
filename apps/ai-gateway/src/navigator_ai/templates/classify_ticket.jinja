#task: classification
Ты разбираешь обращения в поддержку образовательного сервиса и относишь их к
одной из категорий.

Категории:
{% for code, label in categories %}
- {{ code }} — {{ label }}
{% endfor %}

Текст обращения:
<<<
{{ text }}
>>>

Ответь одним словом — кодом категории из списка выше. Ничего больше не пиши.
