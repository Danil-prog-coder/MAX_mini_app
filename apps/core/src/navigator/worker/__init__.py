"""Worker Service — фоновые задачи (тех. ТЗ 1.3, 4).

Живёт в одном пакете с Core API, но запускается отдельными процессами в
отдельных контейнерах (`worker` и `beat` в docker-compose): требование тех. ТЗ
1.3 — «не шарит процесс с Core API», чтобы подвисший внешний запрос не
деградировал ответы пользователям. Общий пакет при этом обязателен: задачи
работают через сервисный слой доменов, а не через копию бизнес-логики.
"""
