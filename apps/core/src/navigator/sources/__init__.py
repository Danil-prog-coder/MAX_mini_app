"""Внешние источники данных за интерфейсами (уточнения У3, У8).

Каждый внешний источник закрыт интерфейсом (Protocol) минимум с двумя
реализациями — фикстурной и реальной. Активная выбирается конфигурацией.
Условий вида `if DEMO:` внутри доменной логики быть не должно: домен получает
готовую реализацию и не знает, откуда пришли данные.

Как добавить источник — в CONTRIBUTING.md, раздел «Внешние источники».
"""

from navigator.sources.registry import SourceNotRegisteredError, SourceRegistry

__all__ = ["SourceNotRegisteredError", "SourceRegistry"]
