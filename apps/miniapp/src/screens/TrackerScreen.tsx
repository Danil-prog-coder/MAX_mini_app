/**
 * Трекер конкурсных списков — экран-заглушка оболочки.
 *
 * Содержимое приходит вертикальным срезом блока (PLAN.md, шаг 6).
 */
import { ScreenStub } from '@/shared/ui/ScreenStub';

export function TrackerScreen() {
  return (
    <ScreenStub
      title="Трекер конкурсных списков"
      note="Список отслеживаемых вузов и пустое состояние с переходом к подбору (ТЗ 2, блок 3). Вузы попадают сюда из карточки вуза."
    />
  );
}
