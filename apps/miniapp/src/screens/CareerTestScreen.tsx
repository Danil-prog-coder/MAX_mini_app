/**
 * Профориентационный тест — экран-заглушка оболочки.
 *
 * Содержимое приходит вертикальным срезом блока (PLAN.md, шаг 6).
 */
import { ScreenStub } from '@/shared/ui/ScreenStub';

export function CareerTestScreen() {
  return (
    <ScreenStub
      title="Профориентационный тест"
      note="Десять вопросов с вариантами ответов и прогресс-полосой (ТЗ 2, блок 1). Прогресс намеренно не переживает выход в меню: при следующем входе тест начинается заново (ТЗ 1.3)."
    />
  );
}
