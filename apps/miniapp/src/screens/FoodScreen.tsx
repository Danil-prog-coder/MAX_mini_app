/**
 * Где покушать — экран-заглушка оболочки.
 *
 * Содержимое приходит вертикальным срезом блока (PLAN.md, шаг 6).
 */
import { ScreenStub } from '@/shared/ui/ScreenStub';

export function FoodScreen() {
  return (
    <ScreenStub
      title="Где покушать"
      note="Топ-5 точек питания в пешей доступности от корпуса вуза по данным Яндекс.Карт (ТЗ 2, блок 7; уточнение по картам в тех. ТЗ 9.2). Координаты берутся из профиля, повторный ввод не требуется."
    />
  );
}
