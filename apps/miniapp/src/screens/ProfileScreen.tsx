/**
 * Мой профиль — экран-заглушка оболочки.
 *
 * Содержимое приходит вертикальным срезом блока (PLAN.md, шаг 6).
 */
import { ScreenStub } from '@/shared/ui/ScreenStub';

export function ProfileScreen() {
  return (
    <ScreenStub
      title="Мой профиль"
      note="Карточка профиля: имя, сегмент статуса, вуз, группа, результат теста, баллы активности, блок верификации и настройки уведомлений (ТЗ 1.3, уточнения У7, У9). Профиль — единственный источник правды о пользователе, остальные блоки его только читают."
    />
  );
}
