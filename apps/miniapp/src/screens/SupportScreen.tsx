/**
 * Поддержка — экран-заглушка оболочки.
 *
 * Содержимое приходит вертикальным срезом блока (PLAN.md, шаг 6).
 */
import { ScreenStub } from '@/shared/ui/ScreenStub';

export function SupportScreen() {
  return (
    <ScreenStub
      title="Поддержка"
      note="Тип обращения, свободный текст и мгновенная типовая отписка, выбранная случайно из пула для этого типа (ТЗ 2, блок 8). Пятнадцать текстов отписок переносятся из макета дословно."
    />
  );
}
