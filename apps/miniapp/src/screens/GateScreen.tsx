/**
 * Нужен статус студента — экран-заглушка оболочки.
 *
 * Содержимое приходит вертикальным срезом блока (PLAN.md, шаг 6).
 */
import { ScreenStub } from '@/shared/ui/ScreenStub';

export function GateScreen() {
  return (
    <ScreenStub
      title="Нужен статус студента"
      note="Маршрут вместо отказа: разделы «Расписание и дедлайны» и «Где покушать» требуют статуса «Студент» и заполненного вуза, поэтому экран ведёт в профиль, а не сообщает «недоступно» (ТЗ 1.3, 4.2, 7.2)."
    />
  );
}
