/**
 * Результат теста — экран-заглушка оболочки.
 *
 * Содержимое приходит вертикальным срезом блока (PLAN.md, шаг 6).
 */
import { ScreenStub } from '@/shared/ui/ScreenStub';

export function CareerTestResultsScreen() {
  return (
    <ScreenStub
      title="Результат теста"
      note="Топ-3 направления с процентом совпадения, переходы к подбору вузов и к вакансиям hh.ru, сохранение результата в профиль с начислением +50 баллов (ТЗ 2, блок 1; уточнения У14, У21)."
    />
  );
}
