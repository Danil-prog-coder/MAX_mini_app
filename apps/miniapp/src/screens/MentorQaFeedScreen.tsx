/**
 * Лента вопросов — экран-заглушка оболочки.
 *
 * Содержимое приходит вертикальным срезом блока (PLAN.md, шаг 6).
 */
import { ScreenStub } from '@/shared/ui/ScreenStub';

export function MentorQaFeedScreen() {
  return (
    <ScreenStub
      title="Лента вопросов"
      note="Лента вопросов и ответов выбранного вуза с лайками (ТЗ 5.5–5.8). Подпись автора ответа — только имя плюс статус месяца, если он есть (уточнение У16); лайки баллов не начисляют (уточнение У22)."
    />
  );
}
