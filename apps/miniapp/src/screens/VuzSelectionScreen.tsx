/**
 * Подбор вуза по баллам ЕГЭ — экран-заглушка оболочки.
 *
 * Содержимое приходит вертикальным срезом блока (PLAN.md, шаг 6).
 */
import { ScreenStub } from '@/shared/ui/ScreenStub';

export function VuzSelectionScreen() {
  return (
    <ScreenStub
      title="Подбор вуза по баллам ЕГЭ"
      note="Мультивыбор предметов и ввод баллов с проверкой диапазона 0–100 (ТЗ 2, блок 2). Введённые баллы переживают выход в меню в рамках сессии (ТЗ 2.4)."
    />
  );
}
