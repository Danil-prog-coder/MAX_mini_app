/**
 * Позиция в списке — экран-заглушка оболочки.
 *
 * Содержимое приходит вертикальным срезом блока (PLAN.md, шаг 6).
 */
import { ScreenStub } from '@/shared/ui/ScreenStub';

export function TrackerDetailScreen() {
  return (
    <ScreenStub
      title="Позиция в списке"
      note="Ввод текущей позиции, сравнение с прошлой проверкой, динамика за неделю и напоминания о датах приёма (ТЗ 3.5–3.7). Введённая позиция переживает выход в меню в рамках сессии."
    />
  );
}
