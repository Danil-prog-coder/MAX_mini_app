/**
 * Спроси у старшекурсника — экран-заглушка оболочки.
 *
 * Содержимое приходит вертикальным срезом блока (PLAN.md, шаг 6).
 */
import { ScreenStub } from '@/shared/ui/ScreenStub';

export function MentorQaScreen() {
  return (
    <ScreenStub
      title="Спроси у старшекурсника"
      note="Выбор темы и вуза, текст вопроса и анонимная публикация (ТЗ 2, блок 5). Спросить может любой, ответить — только привязанный к вузу и прошедший верификацию (уточнения У15, У17)."
    />
  );
}
